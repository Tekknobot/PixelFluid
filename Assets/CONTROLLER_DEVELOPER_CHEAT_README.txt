CONTROLLER DEVELOPER CHEAT + MENU BUTTON PATCH

Enter while the title or pause menu is visible:
UP, UP, LEFT, RIGHT, LEFT, RIGHT, LB, RB

Behavior:
- Each step must be entered within one second.
- D-pad directions are required; analog stick movement is ignored.
- A wrong input resets the sequence.
- Unlock is saved permanently with PlayerPrefs.
- The final RB opens the Developer Menu.
- A real DEVELOPER button is inserted between Controls and Quit.
- The button is hidden until unlocked, then appears immediately and on later launches.
- Selecting the Developer button opens the existing developer overlay.
- F10 remains available after unlock.
